{
  "name": "chat",
  "version": "0.0.1",
  "description": "socket.io driven chat app",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^4.1.3"
  },
  "scripts": {
    "serve": "nodemon server",
    "build":"npm install && node server"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
